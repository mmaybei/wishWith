import { CardWrapper, CardImage, CardContent } from "./CardStyle";

const Card = () => {
  return (
    <CardWrapper>
      <CardImage />
      <CardContent />
    </CardWrapper>
  );
};

export default Card;
